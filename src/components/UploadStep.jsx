import {useState} from "react"; import {supabase} from "../lib/supabaseClient"; export default function UploadStep({onImageSelected}){const [uploading,setUploading]=useState(false);const [error,setError]=useState(null);const handleUpload=async(e)=>{const file=e.target.files?.[0];if(!file)return;setUploading(true);setError(null);try{const {data:session}=await supabase.auth.getSession();const userId=session?.user?.id;if(!userId)throw new Error("Not logged in");const fileName=`${userId}/${Date.now()}.jpg`;const {data,error:uploadError}=await supabase.storage.from("item-images").upload(fileName,file);if(uploadError)throw uploadError;onImageSelected({filePath:data.path,url:supabase.storage.from("item-images").getPublicUrl(data.path).data.publicUrl});}catch(err){setError(err.message);}finally{setUploading(false);}};return(<div style={{padding:"20px",textAlign:"center"}}><h2>Upload tøjstykke</h2><input type="file" onChange={handleUpload} accept="image/*" disabled={uploading}/>{uploading&&<p>Uploader...</p>}{error&&<p style={{color:"red"}}>Error: {error}</p>}</div>);}